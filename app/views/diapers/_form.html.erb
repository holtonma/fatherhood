<%= form_for(@diaper) do |f| %>
  <% if @diaper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diaper.errors.count, "error") %> prohibited this diaper from being saved:</h2>

      <ul>
      <% @diaper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :result %><br />
    <%= f.text_field :result %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_field :note %>
  </div>
  <div class="field">
    <%= f.label :diaper_change_time %><br />
    <%= f.datetime_select :diaper_change_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
